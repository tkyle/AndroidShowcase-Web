@using AndroidShowcase.Business.Entities
@model IEnumerable<Note>

@{
    ViewBag.Title = "Notes";
}

<h1>Good Morning, Wife.</h1>

<hr/>

@foreach (var n in Model)
{
    <div>
        <h3>@n.NoteId</h3>
        @n.NoteTitle
        <h4>@n.NoteContent</h4>
    </div>
}

